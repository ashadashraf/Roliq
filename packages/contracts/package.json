{
  "name": "@roliq/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/validator.ts"
  },
  "scripts": {
    "build": "redocly lint openapi.yaml && tsc --noEmit",
    "lint": "redocly lint openapi.yaml && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "redocly lint openapi.yaml && vitest run"
  },
  "devDependencies": {
    "@redocly/cli": "^2.34.0",
    "@types/node": "^22.20.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.6"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
