{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.roliq.com/events/resume.parse.requested.v1.json",
  "title": "resume.parse.requested.v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "eventType",
    "eventVersion",
    "eventId",
    "occurredAt",
    "correlationId",
    "organizationId",
    "parsingJobId",
    "resumeId",
    "sourceVersionId",
    "sourceFileObjectId",
    "sourceArtifact",
    "outputSchemaVersion",
    "attempt"
  ],
  "properties": {
    "eventType": { "const": "resume.parse.requested" },
    "eventVersion": { "const": 1 },
    "eventId": { "type": "string", "format": "uuid" },
    "occurredAt": { "type": "string", "format": "date-time" },
    "correlationId": { "type": "string", "format": "uuid" },
    "organizationId": { "type": "string", "format": "uuid" },
    "parsingJobId": { "type": "string", "format": "uuid" },
    "resumeId": { "type": "string", "format": "uuid" },
    "sourceVersionId": { "type": "string", "format": "uuid" },
    "sourceFileObjectId": { "type": "string", "format": "uuid" },
    "sourceArtifact": {
      "$ref": "https://contracts.roliq.com/common/artifact-pointer.v1.schema.json"
    },
    "outputSchemaVersion": { "const": "resume-document.v1" },
    "attempt": { "type": "integer", "minimum": 1, "maximum": 3 }
  }
}
